# Architecture Studio

Minimal read-only host for foundational spike 7. It renders the deterministic
Graph IR candidate through `@grafting/graph-x6` and `@grafting/x6-canvas`.

This is not the completed I-006 application and the candidate schema is not an
accepted architectural contract.
