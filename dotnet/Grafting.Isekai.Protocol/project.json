{
  "name": "isekai-dotnet-protocol",
  "root": "dotnet/Grafting.Isekai.Protocol",
  "projectType": "library",
  "tags": ["scope:engine", "lang:csharp", "platform:desktop", "type:contracts"],
  "implicitDependencies": ["engine-domain-core"],
  "metadata": {
    "graphIr": {
      "_comment": "Placeholder only -- no Graph IR v1 schema exists yet. Superseded once a real Graph IR extractor lands (DEC-028).",
      "id": "isekai-dotnet-protocol",
      "kind": "contracts",
      "tags": ["scope:engine", "lang:csharp", "platform:desktop"]
    }
  },
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "dotnet build dotnet/Grafting.Isekai.Protocol"
      },
      "dependsOn": ["engine-domain-core:generate"],
      "cache": true,
      "inputs": [
        "{projectRoot}/**/*",
        "{workspaceRoot}/System.sln",
        "{workspaceRoot}/Directory.Build.props",
        "{workspaceRoot}/Directory.Packages.props",
        "{workspaceRoot}/global.json"
      ]
    }
  }
}
