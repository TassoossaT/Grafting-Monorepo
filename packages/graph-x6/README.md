# `@grafting/graph-x6`

Graph IR-specific adapter for the Architecture Studio. It maps the spike schema
to the generic `@grafting/x6-canvas` API and never owns derived facts.

The current `0.1-spike` schema is experimental and must not be represented as
the accepted Graph IR v1 from I-002.
