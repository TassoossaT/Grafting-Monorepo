{
  "name": "@grafting/graph-x6",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@grafting/x6-canvas": "workspace:*"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
