# AGENTS.md -- `@grafting/isekai-wasm`

Scope-local addendum to the root `AGENTS.md`. Root rules still apply.

Never hand-edit `pkg/` -- it is generated by `wasm-pack` from
`libs/isekai/wasm-bridge` and gitignored. If something here is wrong,
fix the Rust source and regenerate (see `README.md`).

Do not add domain logic or TypeScript re-implementations of anything in
`grafting-isekai-wasm`/`grafting-domain-core` here (DEC-001).
