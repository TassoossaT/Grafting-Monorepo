# AGENTS.md -- `@grafting/vtt-generation-wasm`

Scope-local addendum to the root `AGENTS.md`. Root rules still apply.

Never hand-edit `pkg/` -- it is generated by `wasm-pack` from
`libs/vtt/generation-wasm` and gitignored. If something here is wrong, fix
the Rust source and regenerate (see `README.md`).

Do not add domain logic or TypeScript re-implementations of anything in
`grafting-vtt-generation-wasm` here. This package (and the crate behind it)
is scoped to pipeline step 1 (the continuous heightmap seed) only -- see
`libs/vtt/generation-wasm/AGENTS.md`.
