{
  "name": "@grafting/isekai-wasm",
  "version": "0.0.0",
  "private": true,
  "description": "grafting-isekai-wasm compiled to Wasm via wasm-pack, co-located with its Rust source (master source S9.2, DEC-055/ADR-0017). No domain logic here -- see src/ for the Rust source. `pkg/` is generated by this package's own postinstall script and gitignored; consumers depend on this like any other workspace package (workspace:*), no separate technical package involved.",
  "type": "module",
  "main": "./pkg/grafting_isekai_wasm.js",
  "types": "./pkg/grafting_isekai_wasm.d.ts",
  "files": ["pkg"],
  "scripts": {
    "postinstall": "wasm-pack build --target web --out-dir pkg"
  }
}
