{
  "name": "@grafting/vtt-generation-wasm",
  "version": "0.0.0",
  "private": true,
  "description": "grafting-vtt-generation-wasm compiled to Wasm via wasm-pack, co-located with its Rust source (DEC-055/ADR-0017). No domain logic here -- see src/ for the Rust source. `pkg/` is generated by this package's own postinstall script and gitignored; consumers depend on this like any other workspace package (workspace:*), no separate technical package involved.",
  "type": "module",
  "main": "./pkg/grafting_vtt_generation_wasm.js",
  "types": "./pkg/grafting_vtt_generation_wasm.d.ts",
  "files": ["pkg"],
  "scripts": {
    "postinstall": "wasm-pack build --target web --out-dir pkg"
  }
}
