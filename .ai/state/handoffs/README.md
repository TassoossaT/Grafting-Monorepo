# Handoff records

Handoffs are immutable JSON records validated against
`.ai/contracts/handoff.schema.json`. Use the naming and acknowledgement rules
in `.ai/coordination/PROTOCOL.md`. This README keeps the directory meaningful
before the first real cross-provider handoff is required.
