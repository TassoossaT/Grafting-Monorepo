{
  "schema_version": 1,
  "task_id": "G-DOCS-QUALITY-CHECK-SKILL",
  "title": "Link the per-project doc-regen rule into AGENTS.md and add a docs-quality-check skill that reviews generated docs and suggests generator fixes",
  "status": "completed",
  "owner": "claude",
  "revision": 2,
  "updated_at": "2026-07-31T01:00:00-03:00",
  "objective": "Owner-directed, explicit approval given in-conversation for introducing a new skill (root AGENTS.md's mandatory rules gate this: 'introduce a tool, agent, skill, or MCP without need and evaluation' is forbidden without it; 'changing the protocol, registries, policies, hooks, permissions, skills, or MCPs requires a separate task and explicit owner approval' -- this task record plus the owner's direct request satisfies both). Two parts: (1) cross-reference the G-DOCS-PER-PROJECT-REGEN-RULE completion step from root AGENTS.md, the first file every agent reads, not just PROTOCOL.md three files deep; (2) add a new project-scoped Claude Code skill (.claude/skills/docs-quality-check/SKILL.md, verified file-format/frontmatter against the real Claude Code skills spec via the claude-code-guide agent before authoring, not guessed) that re-analyzes a just-regenerated docs/generated/api/ file for size/emptiness/noise-ratio/formatting problems -- the same categories of real bug already found and fixed by hand this session (raw JSON bloat, derive-impl noise, full-path module names) -- and reports a concrete suggested generator fix rather than hand-editing the generated file. Uses disallowed-tools: Write, Edit in the skill's own frontmatter to make 'suggest, do not apply' a real tool-level constraint, not just an instruction that could be ignored. PROTOCOL.md rule 6 is extended to name this skill as the step after regenerating scoped docs, before marking a task complete.",
  "affected_paths": [
    ".ai/state/tasks/G-DOCS-QUALITY-CHECK-SKILL.json",
    "AGENTS.md",
    ".ai/coordination/PROTOCOL.md",
    ".claude/skills/docs-quality-check/SKILL.md",
    "tools/scripts/README.md",
    "tools/rust-api-docgen/src/main.rs",
    "docs/generated/api/rust/grafting-graph-core.md",
    "docs/generated/api/rust/grafting-compute-api.md",
    "docs/generated/api/rust/grafting-compute-cpu.md",
    "docs/generated/api/rust/grafting-domain-core.md",
    "docs/generated/api/rust/grafting-isekai-capi.md",
    "docs/generated/api/rust/grafting-isekai-wasm.md"
  ],
  "blockers": [],
  "validations": [
    "Verified the real Claude Code skills spec (path convention, frontmatter fields, disallowed-tools syntax, description-driven auto-invocation) via a dedicated claude-code-guide agent call before authoring the file, not assumed.",
    "Skill frontmatter extracted and manually inspected (YAML parses cleanly: name, description, argument-hint, disallowed-tools).",
    "Live invocation attempted in this session (`Skill` tool, docs-quality-check) correctly returned 'Unknown skill' -- expected, since the skill listing loads at session start and this file was created mid-session; not a defect in the file itself.",
    "Real dry-run performed by hand, following the skill's own written instructions exactly, against docs/generated/api/rust/*.md: found a genuine bug -- grafting-domain-core.md (93 KB, ~5x the sibling median) had 250 of 671 entries (37%) as unfiltered generic impl-block noise from FlatBuffers-generated builder types, because is_undocumented_derive_noise's `starts_with(\"impl \")` check (with a trailing space) does not match the generic form `impl<'a, ...>` (no space before `<`). This is precisely the class of bug the skill exists to catch.",
    "Owner confirmed applying the fix (not just recording the suggestion). Fixed is_undocumented_derive_noise to also match `impl<`; cargo build -p grafting-rust-api-docgen compiles clean.",
    "Regenerated all 6 Rust docs: total dropped 153 KB -> 120 KB; grafting-domain-core.md specifically 93 KB -> 66.6 KB; `grep -c '^### `impl' grafting-domain-core.md` confirmed 0 remaining (was 250).",
    "--check converges clean immediately after regenerating.",
    "pnpm run docs:check: full 7-step chain passes end to end.",
    "AGENTS.md's 'introduce a tool, agent, skill, or MCP without need and evaluation' and 'changing the protocol, registries, policies, hooks, permissions, skills, or MCPs requires a separate task and explicit owner approval' gates: satisfied by this dedicated task record plus the owner's direct, explicit in-conversation request."
  ],
  "artifacts": [
    "AGENTS.md (documentation completion criterion now cross-references the regen rule and the skill)",
    ".ai/coordination/PROTOCOL.md (rule 6 extended: run docs-quality-check after regenerating, before completing)",
    ".claude/skills/docs-quality-check/SKILL.md (new skill, disallowed-tools: Write, Edit)",
    "tools/scripts/README.md (cross-referenced in the API-reference extractors section)",
    "tools/rust-api-docgen/src/main.rs (is_undocumented_derive_noise now also matches the generic-impl form, a real bug the new skill found on its first dry run)",
    "docs/generated/api/rust/*.md (all 6 regenerated, domain-core.md 93 KB -> 66.6 KB)"
  ],
  "risks": [
    "Skill file format/frontmatter (path convention, disallowed-tools syntax, description-driven auto-invocation) verified against the real current Claude Code skills spec via a dedicated claude-code-guide agent call before writing the file, not assumed from general knowledge -- avoids shipping a skill Claude Code would not actually discover or load correctly.",
    "disallowed-tools: Write, Edit only holds while this specific skill is the active turn (clears on the next user message, per the verified spec) -- it constrains the skill's own review turn, not the agent's ability to act on the suggestion afterward under a real task, which is intentional (the fix itself still goes through the normal claim/affected_paths process, same as every other generator change this session).",
    "The skill's problem categories (size outlier, empty output, undocumented-noise ratio, formatting artifacts) are exactly the four real bug classes found by hand this session, not a speculative/generic checklist -- deliberately narrow and simple per the owner's explicit 'simples' framing rather than a large general-purpose doc-linter."
  ],
  "next_responsible_party": "claude"
}
